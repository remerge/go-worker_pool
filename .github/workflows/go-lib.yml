name: Go App
on:
  pull_request:
  push:
    branches:
    - master
    - main
    - production

jobs:
  go-checks:
    uses: remerge/workflows/.github/workflows/go-checks.yml@allow_use_no_ssh_key
    with:
      goprivate: ""
